plugins {
    id 'application'
}

application {
    mainClass = 'de.lmu.ifi.sosylab.client.GameClient'
}

dependencies {
    implementation project(':shared')
}

jar {
    manifest {
        attributes 'Main-Class': 'de.lmu.ifi.sosylab.client.GameClient'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}